version: '3.8'

services:
  kids-points:
    build: .
    container_name: kids-points
    ports:
      - "3002:3002"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
